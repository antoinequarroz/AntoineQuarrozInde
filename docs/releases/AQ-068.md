# AQ-068 — Préparation de release

## Périmètre

Livrer la porte E2E mobile du back-office commercial : authentification MFA,
parcours clients → devis → factures → paiements, états d'interface, accessibilité
tactile/clavier et refus explicite de tout test ignoré. Les diagnostics d'échec
authentifiés sont limités à un rapport JSON expurgé.

## Preuves avant fusion

- [x] Plan validé : `docs/plans/AQ-068.md` lié à Linear `ANT-18`.
- [x] Revue : `Max severity: none`, `Ship allowed: yes` dans
  `docs/reviews/AQ-068.md`.
- [x] 658/658 tests Vitest réussis.
- [x] 40/40 scénarios Playwright desktop et mobile réussis, zéro skip.
- [x] Typecheck et build Nuxt/Nitro réussis.
- [x] Défaillance contrôlée : code 1 attendu, rapport expurgé présent, aucun
  `trace.zip`, PNG ou WebM authentifié.
- [x] CI de la PR #131 réussie sur `94cf29e` : `quality`, `accessibility`,
  `database`, `seo-quality` et GitGuardian sont verts.

## Migrations, données et sauvegarde

AQ-068 ne contient aucune migration, modification de schéma, RLS, RPC, donnée
métier ou permission. La porte de production doit confirmer qu'aucune migration
n'est en attente. Aucun backup spécifique n'est requis; le mécanisme chiffré
existant reste inchangé et le scénario E2E ne manipule que `AQ E2E Sandbox` avec
un nettoyage par identifiants exacts.

## Contrat d'environnement

Aucune valeur n'est consignée. La release dépend uniquement des variables déjà
gérées dans l'environnement GitHub `Production` :

- `VPS_HOST`, `VPS_USER`, `VPS_PROJECT_DIR` ;
- `VPS_SSH_PRIVATE_KEY`, `VPS_KNOWN_HOSTS` ;
- `SUPABASE_ACCESS_TOKEN`, `SUPABASE_PROJECT_REF` ;
- `SUPABASE_BACKUP_AGE_RECIPIENT` ;
- `HERMES_READ_TOKEN`, `HERMES_PUBLISH_TOKEN` ;
- `E2E_ADMIN_EMAIL`, `E2E_ADMIN_PASSWORD`, `E2E_ADMIN_TOTP_SECRET`.

## Livraison et vérifications

- [ ] Fusionner la PR #131 uniquement si la CI du dernier commit reste verte.
- [ ] Laisser l'environnement GitHub `Production` promouvoir le SHA exact.
- [ ] Confirmer l'absence de migration en attente avant le déploiement SSH.
- [ ] Vérifier `/api/version` sur le SHA fusionné et `/api/health` entièrement vert.
- [ ] Exiger le succès des preuves HTTP, SEO, sécurité et données structurées.
- [ ] Exiger 40/40 E2E de production, zéro skip, avec le compte sandbox.
- [ ] Passer `ANT-18` à Done seulement après toutes ces preuves.

## Reprise

Le changement est applicatif et CI uniquement. En cas d'échec du candidat, le
script de release restaure automatiquement l'image Docker `previous`. Aucune
restauration SQL ni suppression de données n'est nécessaire.

## État

Aucun blocage avant fusion. La mise en production a été explicitement approuvée
par Antoine le 15 septembre 2026; elle reste fail-closed sur toute porte rouge.
